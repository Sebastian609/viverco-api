RewriteEngine On

# Redirigir todo a /public/
RewriteCond %{REQUEST_URI} !^/public/
RewriteRule ^(.*)$ /public/$1 [L]

# Opcional: redirigir la raíz directamente al index de Laravel
DirectoryIndex public/index.php
